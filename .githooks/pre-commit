#!/bin/sh
# Auto-bump the patch version on every commit so the app version always changes.
# Enabled via: git config core.hooksPath .githooks  (run automatically by `npm install`).
set -e

node scripts/bump-version.mjs
git add package.json
